# main.py

from mypackage.module1 import add_numbers
from mypackage.module2 import multiply_numbers

# Using the add_numbers function from module1
sum_result = add_numbers(5, 7)
print(f"The sum of 5 and 7 is: {sum_result}")

# Using the multiply_numbers function from module2
product_result = multiply_numbers(4, 6)
print(f"The product of 4 and 6 is: {product_result}")
